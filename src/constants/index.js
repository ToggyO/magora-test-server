const ROUTES = require('./routes');
const INFO = require('./info');
const RESPONSE_CODES = require('./code');
const ERROR_MESSAGES = require('./error_messages');
const RESPONSE_STATUSES = require('./statuses');

module.exports = {
	ROUTES,
	INFO,
	RESPONSE_CODES,
	ERROR_MESSAGES,
	RESPONSE_STATUSES,
};
